import React from "react";
import "./Instruction.css";

const Instruction = () => {
  return (
    <div>
      Press the "add card" button to add the new Card. Use the "sort card"
      button to sort the Card by the increase. Press an X icon on the top right
      to delete them.
    </div>
  );
};

export default Instruction;
